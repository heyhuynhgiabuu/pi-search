# v0.2.1

Fixes the `codesearch` tool after live smoke testing against the current Exa MCP endpoint.

## Fixed

- repaired `codesearch` to use Exa's currently available `web_search_exa` backend
- removed dependency on the missing `get_code_context_exa` MCP tool

## Verified

Live smoke tests were run against all bundled tools:

- `websearch` — working
- `codesearch` — working after backend fix
- `context7 resolve` — working
- `context7 query` — working
- `grepsearch` — reached external grep.app rate limiting during the test window

## Notes

- README updated to reflect the actual codesearch implementation
- no breaking API changes to the `codesearch` tool interface
